import { NetworkErrorComponent } from "@/components";
import React from "react";

const NetworkError: React.FC = () => {
  return (
    <div>
      <NetworkErrorComponent />
    </div>
  );
};

export default NetworkError;
